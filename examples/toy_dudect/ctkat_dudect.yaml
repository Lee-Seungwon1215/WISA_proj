project:
  name: toy_dudect
  language: c
  root: .

build:
  # Sources are compiled directly by the dudect harness step; no separate make.
  command: "true"
  workdir: .

dudect:
  enabled: true
  measurements: 50000
  warmup: 500
  batches: 10
  clock: monotonic       # rdtsc requires native x86 — see Phase 4 notes
  seed: 0xC0FFEE
  threshold_warning: 4.5
  threshold_fail: 10.0
  workdir: .
  generated_dir: ./_generated_dudect
  compiler:
    cc: gcc
    cflags: [-O2, -g, -fno-omit-frame-pointer]
  harnesses:
    - name: leaky
      template: generic
      extra_headers: [leaky.h]
      include_dirs: [include]
      sources: [src/leaky.c]
      function: leaky_function
      return_type: int
      args: [secret, "sizeof(secret)"]
      buffers:
        - {name: secret, size: "16", role: secret}

    - name: safe
      template: generic
      extra_headers: [leaky.h]
      include_dirs: [include]
      sources: [src/leaky.c]
      function: safe_function
      return_type: int
      args: [secret, "sizeof(secret)"]
      buffers:
        - {name: secret, size: "16", role: secret}

report:
  output_dir: ./reports
