services:
  ctkat-dev:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    image: ctkat-dev:latest
    container_name: ctkat-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
    command: /bin/bash
