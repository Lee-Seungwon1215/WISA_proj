--- a/clean/poly.c
+++ b/clean_kyberslash2/poly.c
@@ -1,3 +1,12 @@
+/* ============================================================================
+ * FROZEN KyberSlash2-ONLY GROUND-TRUTH OVERLAY — do not "fix" this file.
+ *
+ * Derived from the pinned PQClean ML-KEM-768 clean/poly.c. Only
+ * poly_compress() restores the historical secret-dependent /KYBER_Q
+ * expression; poly_tomsg() remains patched against KyberSlash1.
+ * The sibling polyvec.c restores the second KyberSlash2 compression site.
+ * ==========================================================================*/
+
 #include "cbd.h"
 #include "ntt.h"
 #include "params.h"
@@ -19,7 +28,6 @@
 void PQCLEAN_MLKEM768_CLEAN_poly_compress(uint8_t r[KYBER_POLYCOMPRESSEDBYTES], const poly *a) {
     unsigned int i, j;
     int16_t u;
-    uint32_t d0;
     uint8_t t[8];
 
 
@@ -28,12 +36,7 @@
             // map to positive standard representatives
             u  = a->coeffs[8 * i + j];
             u += (u >> 15) & KYBER_Q;
-            /*    t[j] = ((((uint16_t)u << 4) + KYBER_Q/2)/KYBER_Q) & 15; */
-            d0 = u << 4;
-            d0 += 1665;
-            d0 *= 80635;
-            d0 >>= 28;
-            t[j] = d0 & 0xf;
+            t[j] = ((((uint16_t)u << 4) + KYBER_Q / 2) / KYBER_Q) & 15; /* CTKAT-KS2-POLY */
         }
 
         r[0] = t[0] | (t[1] << 4);
--- a/clean/polyvec.c
+++ b/clean_kyberslash2/polyvec.c
@@ -1,3 +1,11 @@
+/* ============================================================================
+ * FROZEN KyberSlash2-ONLY GROUND-TRUTH OVERLAY — do not "fix" this file.
+ *
+ * Derived from the pinned PQClean ML-KEM-768 clean/polyvec.c. Only
+ * polyvec_compress() restores the historical secret-dependent /KYBER_Q
+ * expression. The sibling poly.c restores the poly_compress() KS2 site.
+ * ==========================================================================*/
+
 #include "params.h"
 #include "poly.h"
 #include "polyvec.h"
@@ -14,7 +22,6 @@
 **************************************************/
 void PQCLEAN_MLKEM768_CLEAN_polyvec_compress(uint8_t r[KYBER_POLYVECCOMPRESSEDBYTES], const polyvec *a) {
     unsigned int i, j, k;
-    uint64_t d0;
 
     uint16_t t[4];
     for (i = 0; i < KYBER_K; i++) {
@@ -22,13 +29,7 @@
             for (k = 0; k < 4; k++) {
                 t[k]  = a->vec[i].coeffs[4 * j + k];
                 t[k] += ((int16_t)t[k] >> 15) & KYBER_Q;
-                /*      t[k]  = ((((uint32_t)t[k] << 10) + KYBER_Q/2)/ KYBER_Q) & 0x3ff; */
-                d0 = t[k];
-                d0 <<= 10;
-                d0 += 1665;
-                d0 *= 1290167;
-                d0 >>= 32;
-                t[k] = d0 & 0x3ff;
+                t[k] = ((((uint32_t)t[k] << 10) + KYBER_Q / 2) / KYBER_Q) & 0x3ff; /* CTKAT-KS2-POLYVEC */
             }
 
             r[0] = (uint8_t)(t[0] >> 0);
